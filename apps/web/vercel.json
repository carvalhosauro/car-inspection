{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && CI=true pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && CI=true turbo run build --filter=@vistoria/web",
  "build": {
    "env": {
      "CI": "true",
      "TURBO_FORCE": "true"
    }
  }
}
