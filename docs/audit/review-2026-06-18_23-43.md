# Revisão de higiene de código — 2026-06-18 23:43

**Escopo:** `packages/*`  ·  **Arquivos analisados:** 160

## Resumo

| Severidade | Qtd |
| ---------- | --- |
| Alta       | 5   |
| Média      | 17  |
| Baixa      | 27  |
| **Total**  | **49** |

Por categoria: Magic values 22 · Código morto 7 · Duplicação 7 · N+1 0 · Tipagem 4 · Legibilidade 9

> Top 3 arquivos com mais problemas: `packages/ui/src/organisms/DataTable/DataTable.web.tsx` (7) · `packages/contracts/src/user.ts` (5) · `packages/ui/src/atoms/Badge/Badge.native.tsx` (4)

---

## Achados por arquivo

### `packages/ui/src/organisms/DataTable/DataTable.web.tsx` — peso 7

- **[ALTA] Cast `as BadgeVariant` sem narrowing** — `linha 12` · _Categoria: Tipagem_
  Por quê: `row[col.key]` é tipado como `unknown`; o cast direto para `BadgeVariant` sem guard de runtime significa que qualquer string inválida chega ao `Badge` sem erro de compilação, anulando o sistema de tipos num componente exportado.
  Direção: Adicionar guard de runtime antes do cast, ou aceitar um callback `renderCell` tipado passado pelo chamador.

- **[MÉDIA] Ternários aninhados para tbody** — `linha 37` · _Categoria: Legibilidade_
  Por quê: Três branches consecutivos (loading / empty / data) dentro do JSX têm aparência visual semelhante e são fáceis de mesclar errado ao editar.
  Direção: Extrair cada state em helper nomeado (`renderSkeleton`, `renderEmpty`, `renderRows`) chamado de uma única expressão.

- **[BAIXA] `renderCell` definida dentro do corpo do componente** — `linha 10` · _Categoria: Legibilidade_
  Por quê: A helper é recriada a cada render e fecha sobre `onView`/`onEdit` de forma não-óbvia; também é mais difícil de testar em isolamento.
  Direção: Içar `renderCell` para fora do componente, recebendo `onView` e `onEdit` como parâmetros explícitos.

---

### `packages/contracts/src/user.ts` — peso 5

- **[ALTA] Allowlist de roles hardcoded (`"supervisor"`, `"vistoriador"`)** — `linha 19` · _Categoria: Magic values_
  Por quê: `createUserInput` restringe o role a um tuple literal raw em vez de derivar de `USER_ROLES`. Ao adicionar um novo role no enum, esta guarda fica silenciosamente desatualizada sem erro de compilação.
  Direção: Derivar o conjunto permitido de `USER_ROLES` (filtrando `"superadmin"` e `"gestor"`) ou introduzir `ASSIGNABLE_ROLES` como constante nomeada em `enums.ts`.

- **[MÉDIA] Tamanho mínimo de senha duplicado (`6`)** — `linha 17` · _Categoria: Magic values / Duplicação_
  Por quê: A regra `min(6)` aparece aqui, em `auth.ts:6` e em `tenant.ts:18` — três sítios independentes para a mesma política. Alterar o mínimo requer caça manual às cópias.
  Direção: Declarar `const passwordSchema = z.string().min(6)` uma vez (ex: `auth.ts`) e importar nos demais arquivos.

---

### `packages/ui/src/atoms/Badge/Badge.native.tsx` — peso 4

- **[MÉDIA] Mapas de glifos de ícones duplicados por componente** — `linha 6` · _Categoria: Duplicação_
  Por quê: Quatro mapas `Record<IconKey, string>` independentes existem em `Badge.native.tsx:6`, `AiPhotoResult.native.tsx:6`, `GeoTag.native.tsx:6` e `IconButton.logic.ts:3` — todos mapeando chave string para caractere Unicode para a mesma finalidade (renderização nativa sem lucide-react-native).
  Direção: Centralizar os glifos em `src/native-glyphs.ts` e cada componente importa apenas o slice necessário.

- **[BAIXA] `borderRadius` 9999 (pill) sem token compartilhado** — `linha 19` · _Categoria: Magic values_
  Por quê: O valor `9999` como convenção pill aparece também em `ProgressBar.native.tsx` sem token centralizado; mudanças requerem busca manual.
  Direção: Adicionar `borderRadius.pill = 9999` nos tokens ou constante `PILL_RADIUS` compartilhada.

- **[BAIXA] `paddingVertical 2` sem token** — `linha 21` · _Categoria: Magic values_
  Por quê: Valor abaixo do menor token de espaçamento (`xs = 4`) — dimensão órfã que não pode ser ajustada via sistema de design.
  Direção: Adicionar `spacing.xxs = 2` nos tokens ou derivar de `spacing.xs / 2`.

---

### `packages/db/src/index.ts` — peso 4

- **[MÉDIA] Tamanho do pool de conexões hardcoded (`max: 10`)** — `linha 8` · _Categoria: Magic values_
  Por quê: Parâmetro operacional embutido no código; não pode ser ajustado por ambiente sem editar o fonte, e não há constante nomeada explicando por que 10 foi escolhido.
  Direção: Ler de env var `DB_POOL_MAX` com fallback, ou ao menos extrair `const DB_POOL_MAX = 10` com comentário de dimensionamento.

- **[MÉDIA] Tipo inline opaco para parâmetro `fn` de `txWithTenant`** — `linha 24` · _Categoria: Legibilidade_
  Por quê: `Parameters<Parameters<Database["transaction"]>[0]>[0]` está correto mas exige desembrulhar mentalmente três níveis de utility types para entender que é um objeto de transação Drizzle.
  Direção: Extrair `type DrizzleTx = Parameters<Parameters<Database['transaction']>[0]>[0]` como export nomeado e usar `fn: (tx: DrizzleTx) => Promise<T>`.

---

### `packages/ui/src/atoms/Select/Select.native.tsx` — peso 3

- **[ALTA] Shell de campo read-only duplicado entre Select e DatePicker** — `linha 6` · _Categoria: Duplicação_
  Por quê: `Select.native.tsx:6-22` e `DatePicker.native.tsx:8-24` contêm blocos de `StyleSheet` quase idênticos (wrapper, label, field, value, placeholder, error) e estruturas de render idênticas. Uma mudança visual exige edição nos dois arquivos.
  Direção: Extrair um `NativeFieldShell` que aceita label, value, placeholder, errorMessage; Select e DatePicker delegam a ele.

---

### `packages/ui/src/atoms/Input/Input.web.tsx` — peso 3

- **[ALTA] Função de render com três branches estruturalmente distintos** — `linha 6` · _Categoria: Legibilidade_
  Por quê: O corpo do componente contém ternário aninhado (select → datepicker → text) que renderiza três elementos fundamentalmente diferentes em uma função, violando responsabilidade única e tornando o fluxo difícil de seguir.
  Direção: Extrair cada branch em sub-componente privado (`SelectField`, `DateField`, `TextField`) e fazer dispatch no pai.

---

### `packages/ui/src/molecules/UploadArea/UploadArea.native.tsx` — peso 3

- **[ALTA] Todos os props ignorados na implementação nativa** — `linha 22` · _Categoria: Código morto_
  Por quê: O componente é tipado como `FC<UploadAreaProps>` mas não desestrutura nada — `state` e `onFiles` são silenciosamente descartados, dando feedback zero ao chamador de que os props são ignorados.
  Direção: Desestruturar e usar os props, ou estreitar o tipo para `FC<{}>` e adicionar comentário explicando que é no-op intencional até um file picker nativo ser implementado.

---

### `packages/contracts/src/pagination.ts` — peso 3

- **[MÉDIA] Tamanho de página padrão (20) e máximo (100) hardcoded** — `linha 5` · _Categoria: Magic values_
  Por quê: Os literais `20` e `100` estão embutidos no schema Zod sem nomes que expliquem a decisão de negócio; outras camadas que precisem dos valores precisam repeti-los ou adivinhar.
  Direção: Declarar `DEFAULT_PAGE_SIZE = 20` e `MAX_PAGE_SIZE = 100` como exports nomeados e referenciar no schema.

- **[BAIXA] `pageSchema` possivelmente não utilizado** — `linha 9` · _Categoria: Código morto_
  Por quê: O `api-client` define inline seus próprios shapes de paginação em vez de chamar `pageSchema`; se nenhum outro consumidor usa o helper, é API pública morta.
  Direção: Buscar `pageSchema` no código; se só referenciado neste arquivo, considerar remover ou documentar como utilitário público intencional.

---

### `packages/contracts/src/inspection.ts` — peso 3

- **[MÉDIA] Strings de decisão de auditoria hardcoded** — `linha 76` · _Categoria: Magic values_
  Por quê: `auditInput` redeclara `["aprovada", "reprovada"]` como tuple literal em vez de referenciar constante compartilhada; sobrepõe `INSPECTION_STATUSES` criando taxonomia paralela que pode divergir.
  Direção: Extrair `AUDIT_DECISIONS = ["aprovada", "reprovada"] as const` em `enums.ts` e usar `z.enum(AUDIT_DECISIONS)`.

- **[BAIXA] `auditInput` possivelmente não utilizado** — `linha 75` · _Categoria: Código morto_
  Por quê: O `api-client` não tem endpoint de auditoria e nenhum dos arquivos revisados referencia este schema; se o fluxo de auditoria ainda não existe, é export prematuro.
  Direção: Verificar no codebase completo se alguma rota/serviço consome `auditInput`; se não, manter interno ou anotar com TODO.

---

### `packages/db/src/schema.ts` — peso 3

- **[BAIXA] Status padrão do veículo `"disponivel"` hardcoded** — `linha 47` · _Categoria: Magic values_
  Por quê: O default `"disponivel"` aparece aqui e em `contracts/src/vehicle.ts:24` sem constante compartilhada; renomear o valor no enum não atualiza ambos automaticamente.
  Direção: Introduzir `DEFAULT_VEHICLE_STATUS` em `enums.ts` e referenciar em ambos.

- **[BAIXA] Status padrão de inspeção `"atribuida"` hardcoded** — `linha 86` · _Categoria: Magic values_
  Por quê: Literal no default de coluna sem referenciar `INSPECTION_STATUSES`; uma renomeação do valor diverge silenciosamente.
  Direção: Introduzir `DEFAULT_INSPECTION_STATUS` derivado de `INSPECTION_STATUSES` e usar aqui.

- **[BAIXA] Status padrão de item `"pendente"` hardcoded** — `linha 109` · _Categoria: Magic values_
  Por quê: Mesmo padrão: `"pendente"` hardcoded em vez de derivado de `ITEM_STATUSES`.
  Direção: Introduzir `DEFAULT_ITEM_STATUS` e referenciar.

---

### `packages/contracts/src/auth.ts` — peso 2

- **[MÉDIA] Regra de senha mínima (`min(6)`) duplicada em três arquivos** — `linha 6` · _Categoria: Magic values / Duplicação_
  Por quê: A mesma regra aparece em `auth.ts:6`, `tenant.ts:18` e `user.ts:17`. Os três sítios são independentes — alterar a política requer caça manual a todas as cópias, e uma desatualizada passará silenciosamente.
  Direção: Declarar `const passwordSchema = z.string().min(6)` (ou `MIN_PASSWORD_LENGTH = 6`) em local compartilhado e importar nos três schemas.

---

### `packages/contracts/src/tenant.ts` — peso 2

- **[MÉDIA] Tamanho mínimo de senha duplicado (`6`)** — `linha 18` · _Categoria: Magic values_
  Por quê: Segunda das três ocorrências do literal `min(6)` para senha (ver também `auth.ts:6` e `user.ts:17`).
  Direção: Compartilhar `passwordSchema` como descrito acima.

---

### `packages/ui/src/atoms/Badge/Badge.logic.ts` — peso 2

- **[MÉDIA] Cores de fundo hex hardcoded (`#DCFCE7`, `#DBEAFE`, `#FEF3C7`, `#FEE2E2`, `#F1F5F9`)** — `linha 25` · _Categoria: Magic values_
  Por quê: As cinco cores de background do `BADGE_CONFIG` são literais hex sem tokens correspondentes. As cores de foreground vêm do mapa de tokens, mas os backgrounds são strings avulsas que não acompanham atualizações do sistema de design.
  Direção: Adicionar entradas `successBg`, `primaryBg`, `warningBg`, `errorBg`, `neutralBg` no `tokens/index.ts` e referenciar aqui.

---

### `packages/ui/src/atoms/ProgressBar/ProgressBar.logic.ts` — peso 2

- **[MÉDIA] Limiares de progresso mágicos (`100`, `50`)** — `linha 11` · _Categoria: Magic values_
  Por quê: `resolveProgress` usa `pct >= 100` e `pct >= 50` para escolher cores sem nomes que expliquem se 50% é uma regra de negócio ou escolha estética.
  Direção: Extrair `PROGRESS_COMPLETE_THRESHOLD = 100` e `PROGRESS_WARN_THRESHOLD = 50` como constantes nomeadas no arquivo.

---

### `packages/ui/src/atoms/Input/Input.logic.ts` — peso 2

- **[MÉDIA] `InputOption` duplica `SelectOption`** — `linha 4` · _Categoria: Duplicação_
  Por quê: `Input.logic.ts:4-7` e `Select.logic.ts:1-4` definem `{ label: string; value: string }` com nomes diferentes — mesma forma, mesma intenção, duas definições que podem divergir.
  Direção: Definir um tipo `Option` compartilhado (ex: em tokens ou arquivo de tipos) e importar em ambos.

---

### `packages/ui/src/atoms/Input/Input.native.tsx` — peso 2

- **[MÉDIA] Bloco de render de `label`/`errorMessage` repetido em três componentes nativos** — `linha 26` · _Categoria: Duplicação_
  Por quê: O padrão `{label ? <Text style={s.label}>{label}</Text> : null}` e `{errorMessage ? <Text style={s.error}>{errorMessage}</Text> : null}` aparece idêntico em `Input.native.tsx`, `Select.native.tsx` e `DatePicker.native.tsx`.
  Direção: Absorver no `NativeFieldShell` sugerido acima, ou extrair `NativeFieldLabel` + `NativeFieldError` reutilizáveis.

---

### `packages/ui/src/atoms/Button/Button.web.tsx` — peso 2

- **[MÉDIA] `variantClass` indexado por `string` em vez de `ButtonVariant`** — `linha 5` · _Categoria: Tipagem_
  Por quê: `Record<string, string | undefined>` aceita qualquer chave; uma variante grafada errado mapeia para `undefined` em runtime sem erro de compilação.
  Direção: Tipar como `Record<ButtonVariant, string | undefined>` e importar `ButtonVariant` de `Button.logic`.

---

### `packages/ui/src/domain/GeoTag/GeoTag.web.tsx` — peso 2

- **[MÉDIA] Nullish coalescing redundante após default de destructuring** — `linha 9` · _Categoria: Código morto / Legibilidade_
  Por quê: `status` já tem default `'pending'` no destructuring (linha 8), tornando `status ?? 'pending'` na linha 9 inalcançável e enganoso — implica que `status` poderia ser nullish quando não pode.
  Direção: Remover o `?? 'pending'` e usar `status` diretamente.

---

### `packages/ui/src/domain/GeoTag/GeoTag.native.tsx` — peso 2

- **[MÉDIA] Nullish coalescing redundante após default de destructuring** — `linha 20` · _Categoria: Código morto / Legibilidade_
  Por quê: Mesmo padrão do sibling web: `status` tem default `'pending'` nos parâmetros, tornando `status ?? 'pending'` na linha 20 ruído defensivo enganoso.
  Direção: Remover o `?? 'pending'` e usar `status` diretamente.

---

### `packages/ui/src/domain/UniqueCode/UniqueCode.web.tsx` — peso 2

- **[MÉDIA] Lógica de timer "copied" duplicada entre web e native** — `linha 9` · _Categoria: Duplicação_
  Por quê: `UniqueCode.web.tsx:9-13` e `UniqueCode.native.tsx:33-37` implementam exatamente o mesmo padrão: `useState(false)` + `useEffect` com `setTimeout` de 1500ms para resetar. Se o timeout mudar, dois arquivos precisam ser atualizados.
  Direção: Extrair `useCopied(durationMs?: number)` hook em `UniqueCode.logic.ts` e importar nas duas variantes de plataforma.

---

### `packages/contracts/src/checklist.ts` — peso 2

- **[BAIXA] `createRequirementInput` sem type alias** — `linha 34` · _Categoria: Código morto_
  Por quê: Exportado como schema Zod mas sem `CreateRequirementInput` correspondente, ao contrário de todos os outros schemas do pacote. Consumidores que precisam do tipo não têm nada para importar.
  Direção: Adicionar `export type CreateRequirementInput = z.infer<typeof createRequirementInput>` ou confirmar que a omissão é intencional.

- **[BAIXA] `createChecklistItemInput` sem type alias** — `linha 41` · _Categoria: Código morto_
  Por quê: Mesmo padrão: schema exportado sem type alias derivado, inconsistente com os demais.
  Direção: Adicionar `export type CreateChecklistItemInput = z.infer<typeof createChecklistItemInput>`.

---

### `packages/contracts/src/vehicle.ts` — peso 1

- **[BAIXA] Status padrão do veículo `"disponivel"` duplicado** — `linha 24` · _Categoria: Magic values_
  Por quê: Mesmo literal raw que `schema.ts:47` — dois sítios para o mesmo default sem constante compartilhada.
  Direção: Referenciar `DEFAULT_VEHICLE_STATUS` de `enums.ts`.

---

### `packages/db/src/seed.ts` — peso 1

- **[BAIXA] Função `main` sem docstring de pré-condições** — `linha 5` · _Categoria: Legibilidade_
  Por quê: O seed insere dados assumindo schema migrado e valores de enum específicos, mas não há comentário indicando se é destrutivo, idempotente, ou o que o chamador deve garantir antes de executar.
  Direção: Adicionar JSDoc declarando pré-condições (schema migrado, tabelas preferencialmente vazias) e como invocar.

---

### `packages/api-client/src/index.ts` — peso 1

- **[BAIXA] Tipo de lista paginada inline em vez de usar `pageSchema`** — `linha 19` · _Categoria: Duplicação_
  Por quê: As linhas 19 e 23 escrevem `{ items: T[]; nextCursor: string | null }` inline enquanto `contracts/src/pagination.ts` já exporta `pageSchema` com exatamente essa forma — terceira representação do mesmo conhecimento.
  Direção: Derivar tipo `Page<T>` do `pageSchema` existente e usar nos retornos do cliente.

---

### `packages/ui/src/atoms/Badge/Badge.web.tsx` — peso 1

- **[BAIXA] Mapa lucide de ícones duplicado em Badge e AiPhotoResult** — `linha 6` · _Categoria: Duplicação_
  Por quê: `Badge.web.tsx:6-12` e `AiPhotoResult.web.tsx:6-9` declaram localmente `Record<IconName, typeof CheckCircle2>` usando o mesmo padrão `const Map = { ... }; const Icon = Map[cfg.icon]`.
  Direção: Extrair helper `resolveLucideIcon(name: string)` ou registry global no pacote UI que ambos importem.

---

### `packages/ui/src/atoms/Button/Button.native.tsx` — peso 1

- **[BAIXA] `borderRadius 8` sem token** — `linha 19` · _Categoria: Magic values_
  Por quê: Raio de canto `8` hardcoded sem entrada no mapa de tokens, sem hierarquia nomeada distinguindo do pill radius (`9999`) ou card radius (`12`).
  Direção: Adicionar `borderRadius.button` ou `borderRadius.md` nos tokens e usar aqui.

---

### `packages/ui/src/atoms/DatePicker/DatePicker.logic.ts` — peso 1

- **[BAIXA] Comentário WHAT descrevendo transformação óbvia** — `linha 10` · _Categoria: Legibilidade_
  Por quê: O comentário `// "2026-01-15" -> "15/01/2026"` reafirma o que o nome `formatDisplayDate` e o corpo já tornam evidente; não explica o porquê de não usar `Intl.DateTimeFormat`.
  Direção: Remover o comentário ou substituir por WHY explicando a escolha de transformação manual em vez de `Intl`.

---

### `packages/ui/src/atoms/IconButton/IconButton.logic.ts` — peso 1

- **[BAIXA] `ICON_GLYPH` exportado do arquivo lógico, só usado no native** — `linha 3` · _Categoria: Legibilidade_
  Por quê: O mapa de glifos é uma preocupação exclusivamente nativa (a versão web usa componentes Lucide) mas vive no arquivo lógico compartilhado, misturando dado específico de plataforma com definições de interface compartilhada.
  Direção: Mover `ICON_GLYPH` para `IconButton.native.tsx` onde é de fato consumido.

---

### `packages/ui/src/atoms/ProgressBar/ProgressBar.native.tsx` — peso 1

- **[BAIXA] `borderRadius 9999` e altura de trilha `8` sem tokens** — `linha 7` · _Categoria: Magic values_
  Por quê: Dois literais sem tokens: o pill radius `9999` (duplicado de `Badge.native.tsx`) e a altura de trilha `8` não disponíveis no sistema de design.
  Direção: Usar `PILL_RADIUS` compartilhado e adicionar `size.progressTrackHeight = 8` nos tokens.

---

### `packages/ui/src/domain/UniqueCode/UniqueCode.logic.ts` — peso 1

- **[BAIXA] Regex com prefixo `"VST"` e comprimento `6` hardcoded** — `linha 6` · _Categoria: Magic values_
  Por quê: `/^VST-[A-Z0-9]{6}$/` emite o prefixo da aplicação e o comprimento do código como literais sem nomes explicando o significado de `VST` ou por que 6 caracteres.
  Direção: Extrair `CODE_PREFIX = "VST"` e `CODE_LENGTH = 6` e construir a regex a partir deles.

---

### `packages/ui/src/molecules/Modal/Modal.logic.ts` — peso 1

- **[BAIXA] Parâmetro `p` sem nome descritivo em `resolveModalLabels`** — `linha 17` · _Categoria: Legibilidade_
  Por quê: Nome de letra única exige leitura da anotação de tipo para entender o que está sendo recebido.
  Direção: Renomear `p` para `props` ou `labels`.

---

### `packages/ui/src/molecules/UploadArea/UploadArea.logic.ts` — peso 1

- **[BAIXA] `MAX_BYTES` não refletido nas strings de UI** — `linha 3` · _Categoria: Magic values_
  Por quê: `MAX_BYTES` é exportado como constante nomeada (correto), mas o componente web hardcoda `"10MB"` no label e na mensagem de erro. Se `MAX_BYTES` mudar, as strings de usuário mentem silenciosamente.
  Direção: Derivar o label de `MAX_BYTES` em runtime (ex: `` `${MAX_BYTES / (1024 * 1024)}MB` ``).

---

### `packages/ui/src/molecules/UploadArea/UploadArea.web.tsx` — peso 1

- **[BAIXA] Tipos MIME duplicados no atributo `accept`** — `linha 46` · _Categoria: Magic values_
  Por quê: O `<input accept>` repete `"image/png,image/jpeg"` como string raw enquanto `UploadArea.logic.ts` já exporta a constante `ACCEPTED`; se os tipos aceitos mudarem, dois lugares precisam ser atualizados.
  Direção: Derivar o `accept` de `ACCEPTED.join(",")`.

---

### `packages/ui/src/molecules/VehicleCard/VehicleCard.native.tsx` — peso 1

- **[BAIXA] `borderRadius` (12, 8) e altura de imagem (140) sem tokens** — `linha 13` · _Categoria: Magic values_
  Por quê: Card radius `12`, image radius `8` e image height `140` são literais sem token; nenhuma hierarquia nomeada explica a diferença entre os raios.
  Direção: Introduzir `borderRadius.card = 12`, `borderRadius.image = 8` e `size.vehicleImageHeight = 140` nos tokens.

---

### `packages/ui/src/organisms/BottomNav/BottomNav.logic.ts` — peso 1

- **[BAIXA] Limite de badge overflow (`9`) hardcoded** — `linha 23` · _Categoria: Magic values_
  Por quê: `count > 9 ? "9+"` é uma política de UI sem constante nomeada; se o limite mudar para 99, dois literais precisam ser atualizados em sincronia.
  Direção: Extrair `BADGE_MAX_COUNT = 9` e usar `` count > BADGE_MAX_COUNT ? `${BADGE_MAX_COUNT}+` ``.

---

### `packages/ui/src/organisms/BottomNav/BottomNav.web.tsx` — peso 1

- **[BAIXA] `tab.id` cast para `keyof typeof BOTTOM_NAV_ICONS`** — `linha 27` · _Categoria: Tipagem_
  Por quê: `tab.id` é `string`; o cast suprime o erro de widening, então uma tab adicionada em `DEFAULT_TABS` sem ícone correspondente compila mas renderiza nada, escondendo a omissão.
  Direção: Estreitar `Tab.id` para union literal dos ids conhecidos correspondentes às chaves do mapa de ícones.

---

### `packages/ui/src/organisms/Sidebar/Sidebar.logic.ts` — peso 1

- **[BAIXA] `isActive` trivialmente substituível por inline** — `linha 24` · _Categoria: Legibilidade_
  Por quê: `return linkId === activeId` adiciona indireção sem benefício de legibilidade; o leitor precisa pular para a definição para confirmar que é apenas igualdade.
  Direção: Inlinear a comparação no ponto de uso e remover o helper, ou pelo menos não exportá-lo se só usado internamente.

---

### `packages/ui/src/organisms/Sidebar/Sidebar.web.tsx` — peso 1

- **[BAIXA] `link.id` cast para `keyof typeof SIDEBAR_ICONS`** — `linha 36` · _Categoria: Tipagem_
  Por quê: Mesmo problema do `BottomNav.web.tsx`: `NavLink.id` é `string`; um novo link sem ícone correspondente compila e cai no fallback `Circle` silenciosamente.
  Direção: Estreitar `NavLink.id` para union literal correspondendo às chaves de `SIDEBAR_ICONS`.

---

## Não aplicável

- **N+1:** Não aplicável a nenhum arquivo do escopo. Os pacotes `contracts` e `api-client` são schemas/tipos; o pacote `ui` é componentes React sem acesso a ORM. O pacote `db` contém schema Drizzle e seed, mas nenhum loop com query per-item foi identificado.

---

_Relatório somente informativo — nenhum arquivo foi modificado._
