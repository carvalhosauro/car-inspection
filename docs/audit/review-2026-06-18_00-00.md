# Revisão de higiene de código — 2026-06-18

**Escopo:** `apps/api/src/`  ·  **Arquivos analisados:** 70

## Resumo

| Severidade | Qtd |
| ---------- | --- |
| Alta       | 11  |
| Média      | 32  |
| Baixa      | 13  |
| **Total**  | **56** |

Por categoria: Magic values 16 · Código morto 4 · Duplicação 11 · N+1 0 · Tipagem 17 · Legibilidade 8

> Top 3 arquivos com mais problemas: `modules/inspections/service.ts` (12) · `core/ai/handlers/photo.ts` (5) · `modules/evidences/repo.ts` (4)

## Achados por arquivo

### `apps/api/src/modules/inspections/service.ts` — peso 20

- **[ALTA] Literais de status de domínio espalhados em 5 arquivos** — `linha 177` · _Categoria: Magic Values + Duplicação_
  Por quê: Strings `'atribuida'`, `'em_andamento'`, `'concluida'`, `'conforme'`, `'nao_conforme'`, `'com_pendencias'` aparecem cruas em `inspections/service.ts`, `inspections/repo.ts`, `reports/repo.ts`, `evidences/service.ts` e `evidences/repo.ts`. Um typo ou renomeação exige caçar cada cópia sem segurança em tempo de compilação.
  Direção: Promover a objetos `INSPECTION_STATUS` e `ITEM_STATUS` (ou enums) em módulo compartilhado de constantes.

- **[MÉDIA] `evToDto` duplicada verbatim em `evidences/service.ts`** — `linha 137` · _Categoria: Duplicação + Legibilidade_
  Por quê: Mesma função de mapeamento definida identicamente em `inspections/service.ts:137-159` e `evidences/service.ts:23-45`.
  Direção: Mover `evToDto` para `modules/evidences/dto.ts` e importar nos dois serviços.

- **[MÉDIA] Guard `getInspection + notFound` repetido 5× no mesmo arquivo** — `linha 161` · _Categoria: Duplicação_
  Por quê: O par `const insp = await getInspection(tx, tenantId, id); if (!insp) throw errors.notFound(...)` aparece pelo menos 5 vezes no corpo do arquivo.
  Direção: Extrair `requireInspection(tx, tenantId, id)` que retorna a linha ou lança exceção.

- **[MÉDIA] `finish` mistura três responsabilidades** — `linha ~250` · _Categoria: Legibilidade_
  Por quê: A função faz SELECT ad-hoc de tenant (ignorando a camada repo), gera unique-code e atualiza status — três responsabilidades em um só corpo.
  Direção: Separar em `generateInspectionCode(tx, tenantId)` e `finalizeInspectionStatus(tx, id)`.

- **[MÉDIA] `86_400_000` — milissegundos por dia sem nome** — `linha 276` · _Categoria: Magic Values_
  Por quê: O leitor precisa verificar a conta; não fica claro que representa exatamente 1 dia.
  Direção: Extrair `MS_PER_DAY = 24 * 60 * 60 * 1000` ou importar de módulo de constantes de tempo.

- **[MÉDIA] Prefixo `'VST-'` hardcoded no unique-code** — `linha 201` · _Categoria: Magic Values_
  Por quê: Formato do código de inspeção como string literal inline; qualquer mudança exige caçar o literal.
  Direção: Extrair `INSPECTION_CODE_PREFIX = 'VST'` e encapsular a geração em função auxiliar.

- **[MÉDIA] Parâmetro morto `tenantId` em `myToday`** — `linha 271` · _Categoria: Código Morto_
  Por quê: `tenantId` é aceito mas nunca lido — `listInspectorToday` não o recebe; valor descartado silenciosamente.
  Direção: Remover o parâmetro de `myToday` e atualizar o call-site na rota.

- **[MÉDIA] Resultados Drizzle castados para tipo local `InspectionRow`** — `linha 108` · _Categoria: Tipagem_
  Por quê: Sete call-sites castam resultados Drizzle; drift de schema ficaria invisível ao compilador.
  Direção: Derivar `InspectionRow` do schema via `InferSelectModel` em vez de cast manual.

- **[BAIXA] Fallback de slug literal `'tenant'`** — `linha 200` · _Categoria: Magic Values_
  Por quê: `tenant[0]?.slug ?? "tenant"` retorna a string arbitrária `'tenant'` silenciosamente se não houver tenant.
  Direção: Lançar exceção (estado impossível pós-auth) ou extrair constante `UNKNOWN_TENANT_SLUG`.

- **[BAIXA] `requirementsSnapshot` cast de `unknown` sem validação** — `linha 130` · _Categoria: Tipagem_
  Por quê: JSON vindo do banco é tratado como o tipo esperado sem parse ou guard.
  Direção: Validar com Zod antes de usar o valor.

- **[BAIXA] Cursor-pagination duplicado — achado primário em `users/service.ts`** — ver seção abaixo.

### `apps/api/src/core/ai/handlers/photo.ts` — peso 10

- **[ALTA] SafeSearch aceito como conjunto de literais string inline** — `linha 5` · _Categoria: Magic Values_
  Por quê: `SAFE_OK = new Set(["VERY_UNLIKELY", "UNLIKELY", "POSSIBLE", "UNKNOWN"])` encoda uma política de moderação de conteúdo como literais; a exclusão de `'LIKELY'` e `'VERY_LIKELY'` é uma regra de segurança silenciosa sem nome.
  Direção: Definir `SAFE_SEARCH_ACCEPTED_LIKELIHOODS` como constante nomeada com comentário documentando a política.

- **[MÉDIA] Handler realiza 5 responsabilidades em um único corpo** — `linha ~15` · _Categoria: Legibilidade_
  Por quê: Um único corpo executa chamada Vision, cálculo pHash, verificação safe-search, match de labels e decisão de dedup.
  Direção: Dividir em helpers `callVision`, `checkSafety`, `matchLabels`, `isDuplicate` chamados pelo handler.

- **[MÉDIA] `DEFAULT_MIN_SCORE = 0.6` — threshold de ML opaco** — `linha 7` · _Categoria: Magic Values_
  Por quê: 0.6 é limite de classificação não óbvio; qualquer mudança afeta silenciosamente todo item de evidência foto.
  Direção: Mover `DEFAULT_MIN_SCORE` e `DEFAULT_MIN_HAMMING` para módulo de config de IA compartilhado.

- **[MÉDIA] `let annotation` / `let dedupHash` sem tipo antes do try-catch** — `linha 14` · _Categoria: Tipagem + Legibilidade_
  Por quê: Variáveis declaradas sem anotação de tipo; tipo é implicitamente `any` até atribuição dentro do bloco.
  Direção: Declarar com tipo explícito e valor inicial claro, ou reestruturar para evitar declarações antecipadas.

- **[BAIXA] Guard de bytes ausentes repetido em 3 handlers** — `linha 10` · _Categoria: Duplicação_
  Por quê: `if (!bytes) return { accepted: null, ... }` aparece em `photo.ts:10`, `ocr-km.ts:5` e `ocr-plate.ts:5`.
  Direção: Extrair `guardBytes(bytes?)` que retorna o resultado pendente ou `null`.

### `apps/api/src/core/ai/plate.ts` — peso 8

- **[ALTA] Variável `originalIndex` declarada e nunca lida** — `linha 11` · _Categoria: Código Morto_
  Por quê: Inicializada como `0`, nunca incrementada nem referenciada; somente `cleanedIndex` drive o mapa de índices.
  Direção: Remover a declaração `let originalIndex = 0` inteiramente.

- **[ALTA] Loops de busca Mercosul/OLD copy-paste com diferença apenas no regex** — `linha 25` · _Categoria: Duplicação + Legibilidade_
  Por quê: O corpo do loop (lookup de índice, verificação de boundary `hasBefore`/`hasAfter`, retorno condicional) é copiado em `plate.ts:26-45` e `plate.ts:48-67`; única diferença é o regex testado.
  Direção: Colapsar em `findPlateWithBoundary(cleaned, upper, indexMap, regex)` chamado uma vez por formato.

### `apps/api/src/modules/evidences/repo.ts` — peso 8

- **[MÉDIA] `getVehicle`, `getInspection` e `listItemEvidences` duplicados entre repos** — `linha 23` · _Categoria: Duplicação_
  Por quê: Três fetchers de entidade definidos identicamente em `evidences/repo.ts` e `inspections/repo.ts`; um change precisa ser feito nos dois lugares.
  Direção: Consolidar fetchers compartilhados em módulo repo único importado por ambos.

- **[MÉDIA] String `'photo'` como filtro de kind no DB** — `linha 62` · _Categoria: Magic Values_
  Por quê: `eq(schema.inspectionEvidences.kind, "photo")` — renomeação do valor `ProofKind` quebra o filtro silenciosamente.
  Direção: Referenciar via constante ou tipo `ProofKind` em vez de string literal.

- **[MÉDIA] Status literal `'pendente'` em `insertChildItem`** — `linha 125` · _Categoria: Magic Values_
  Por quê: Mesmo literal de status de domínio espalhado por outros arquivos sem fonte única de verdade.
  Direção: Substituir por `ITEM_STATUS.pendente`.

- **[MÉDIA] `.set(data as never)` suprime mismatch de tipo real** — `linha 98` · _Categoria: Tipagem_
  Por quê: O cast `as never` esconde incompatibilidade de tipo que o Drizzle sinalizaria de outra forma.
  Direção: Corrigir o tipo do parâmetro `data` em `updateEvidence` para corresponder ao schema Drizzle.

### `apps/api/src/modules/vehicles/service.ts` — peso 8

- **[ALTA] Código Postgres `'23505'` duplicado** — `linha 53` · _Categoria: Magic Values + Duplicação_
  Por quê: Mesma string `'23505'` aparece também em `tenants/service.ts:35`; leitor sem background em Postgres não entende o guard, e a duplicação cria dois pontos de manutenção.
  Direção: Extrair `PG_UNIQUE_VIOLATION = '23505'` e `isUniqueViolation(err)` para módulo de utilitários DB.

- **[MÉDIA] `softDelete` define status `'manutencao'` — nome enganoso** — `linha 92` · _Categoria: Magic Values + Legibilidade_
  Por quê: O nome sugere inativação; o código define status para manutenção — comportamento não condiz com o nome.
  Direção: Renomear para `sendToMaintenance` ou ajustar a lógica para refletir exclusão real.

- **[MÉDIA] `status` tipado como `string` em `updateVehicle`** — `linha 44` · _Categoria: Tipagem_
  Por quê: Aceita qualquer string em vez da union de status válidos; drift silencioso se novos status forem adicionados.
  Direção: Tipar com union literal `'ativo' | 'manutencao' | ...` ou usar tipo inferido do schema Drizzle.

### `apps/api/src/core/auth/auth-context.ts` — peso 6

- **[MÉDIA] `let payload` sem anotação de tipo** — `linha 57` · _Categoria: Tipagem_
  Por quê: Declarado sem tipo antes do try-catch; tipo implícito é `any` até a atribuição interna.
  Direção: Declarar como `let payload: JwtPayload | undefined` explicitamente.

- **[MÉDIA] `_pendingTx` acessado via double-cast `as unknown as` repetido 5×** — `linha 72` · _Categoria: Tipagem + Legibilidade_
  Por quê: O cast `(request as unknown as { _pendingTx?: PendingTx })` é frágil e repetido 5 vezes no mesmo arquivo.
  Direção: Adicionar augmentação de módulo Fastify em `types.ts` para eliminar todos os casts.

- **[MÉDIA] Deferred-resolve sem comentário WHY** — `linha ~80` · _Categoria: Legibilidade_
  Por quê: O truque de expor o `resolve` de fora do construtor é não óbvio; ausência de comentário força o leitor a deduzir o motivo.
  Direção: Adicionar comentário curto explicando por que o resolve precisa ser exposto externamente.

### `apps/api/src/modules/users/service.ts` — peso 5

- **[ALTA] Padrão cursor-pagination duplicado em 5 módulos** — `linha 57` · _Categoria: Duplicação_
  Por quê: O bloco `hasMore / slice / nextCursor` é idêntico em `users/service.ts:59-64`, `vehicles/service.ts:72-77`, `checklists/service.ts:107-110`, `inspections/service.ts:261-266` e `tenants/service.ts:47-53`. A mesma regra de negócio codificada 5 vezes.
  Direção: Extrair `buildPage<T>(rows, limit, toItem)` como utilitário compartilhado em `core`.

- **[MÉDIA] `catch` genérico promove todos os erros DB a `409 Conflict`** — `linha ~70` · _Categoria: Legibilidade_
  Por quê: Apenas `23505` deveria gerar `409`; outros erros DB ficam mascarados como conflito.
  Direção: Usar `isUniqueViolation(err)` e relançar o restante.

### `apps/api/src/modules/users/repo.ts` — peso 3

- **[ALTA] Branch `undefined` inalcançável no `where` de `listUsers`** — `linha 26` · _Categoria: Código Morto_
  Por quê: `conditions` sempre é inicializado com pelo menos um elemento; o braço `: undefined` do ternário nunca executa.
  Direção: Simplificar para `const where = and(...conditions)` e remover o ternário.

### `apps/api/src/modules/tenants/service.ts` — peso 3

- **[ALTA] Código Postgres `'23505'` como magic string** — `linha 35` · _Categoria: Magic Values + Duplicação_
  Por quê: Mesma string literal de `vehicles/service.ts`; dois lugares para manter o guard de unicidade.
  Direção: Consolidar em `PG_UNIQUE_VIOLATION = '23505'` compartilhado com helper `isUniqueViolation(err)`.

### `apps/api/src/modules/vehicles/route.ts` — peso 3 _(representa todas as rotas)_

- **[ALTA] Guard `tenantId !== null` duplicado em 25+ call-sites** — `linha 24` · _Categoria: Duplicação_
  Por quê: `if (request.ctx.tenantId === null) throw errors.badRequest(...)` aparece 25+ vezes em `vehicles/route.ts`, `checklists/route.ts`, `inspections/route.ts`, `evidences/route.ts`, `reports/route.ts`, `uploads/route.ts` e `users/route.ts`.
  Direção: Criar `requireTenant(request): string` que lança o erro e retorna o `tenantId` não-nulo.

### `apps/api/src/core/ai/handlers/ocr-km.ts` — peso 3

- **[ALTA] Preâmbulo OCR copiado verbatim em `ocr-plate.ts`** — `linha 5` · _Categoria: Duplicação_
  Por quê: As três etapas de guard (sem bytes → pendente, Vision lança → pendente, sem texto → pendente) são idênticas em `ocr-km.ts:5-13` e `ocr-plate.ts:5-13`.
  Direção: Extrair `callAnnotateText(bytes)` que retorna `{ text }` ou `{ pending: HandlerResult }`.

### `apps/api/src/modules/auth/repo.ts` — peso 3

- **[ALTA] `findUserByEmail` e `findUserById` sem tipo de retorno explícito** — `linhas 4, 13` · _Categoria: Tipagem_
  Por quê: Funções exportadas com retorno Drizzle amplo (incluindo `undefined`) fluem para o serviço de auth sem contrato explícito.
  Direção: Anotar retorno como `Promise<UserRow | undefined>` em ambas.

### `apps/api/src/core/ai/vision.ts` — peso 4

- **[MÉDIA] Imports nomeados usados apenas como tipos** — `linha 1` · _Categoria: Código Morto_
  Por quê: `ImageAnnotatorClient` e `protos` são importados como valores mas aparecem exclusivamente em posições de tipo; a instanciação usa o default export.
  Direção: Converter para `import type { ImageAnnotatorClient, protos }`.

- **[BAIXA] `safeSearch` tipado como `Record<string, string>`** — `linha 19` · _Categoria: Tipagem_
  Por quê: Shape real tem campos conhecidos (`adult`, `violence`, `racy`); tipo genérico perde a informação estrutural.
  Direção: Criar interface concreta `SafeSearchAnnotation` com os campos esperados.

- **[BAIXA] `annotatePhoto` engole erros Vision e retorna fallback que passa safe-search** — `linha ~40` · _Categoria: Legibilidade_
  Por quê: Erro Vision silencioso resulta em `safeSearch = 'UNKNOWN'` em todos os campos, que `SAFE_OK` trata como aprovado — conteúdo potencialmente inadequado passa sem revisão.
  Direção: Relançar o erro para que os callers possam retornar `accepted: null`.

### `apps/api/src/core/ai/phash.ts` — peso 4

- **[MÉDIA] Dimensão de resize `256, 256` sem nome** — `linha 10` · _Categoria: Magic Values_
  Por quê: Resolução de normalização para perceptual hash; sem nome, o leitor não sabe se é arbitrária ou exigida pelo algoritmo blockhash.
  Direção: Extrair `PHASH_RESIZE_SIZE = 256` adjacente à função.

- **[MÉDIA] `bits = 16` no blockhash sem nome** — `linha 19` · _Categoria: Magic Values_
  Por quê: Segundo argumento de `bmvbhash`; deve ser consistente com a lógica de comparação de hash mas não há referência que ligue os dois.
  Direção: Extrair `PHASH_BITS = 16` junto de `PHASH_RESIZE_SIZE`.

### `apps/api/src/modules/checklists/service.ts` — peso 3

- **[MÉDIA] Loop `insertItem + insertRequirement` duplicado em `createTemplate` e `addItem`** — `linha 78` · _Categoria: Duplicação_
  Por quê: Lógica de inserção de requisitos copiada em `checklists/service.ts:84-90` e `checklists/service.ts:131-137`.
  Direção: Extrair `insertItemWithRequirements(tx, tenantId, templateId, item)`.

- **[BAIXA] `config` cast sem validação na fronteira do DTO** — `linha 36` · _Categoria: Tipagem_
  Por quê: `config` tratado como `Record<string, unknown> | null` sem parse; shape real não é garantida.
  Direção: Validar com Zod na conversão JSON → DTO.

- **[BAIXA] `await` sequencial em loop para inserts independentes** — `linha ~85` · _Categoria: Legibilidade_
  Por quê: Inserts que poderiam ser paralelos são aguardados um a um sem explicação da necessidade de serialização.
  Direção: Paralelizar com `Promise.all` ou adicionar comentário se ordenação é necessária.

### `apps/api/src/modules/checklists/repo.ts` — peso 2

- **[MÉDIA] Nove funções exportadas sem tipo de retorno** — `linha 6` · _Categoria: Tipagem_
  Por quê: Callers dependem de inferência ampla do Drizzle; drift de schema não seria capturado pelo compilador.
  Direção: Anotar tipos de retorno explícitos (`Promise<TemplateRow[]>`, etc.) nas funções exportadas.

### `apps/api/src/modules/inspections/repo.ts` — peso 3

- **[MÉDIA] `updateInspection` aceita `Record<string, unknown>` como `data`** — `linha 119` · _Categoria: Tipagem_
  Por quê: Contorna verificação de tipo em nível de coluna do Drizzle; colunas inexistentes falhariam silenciosamente.
  Direção: Tipar com o tipo parcial gerado pelo Drizzle para o schema de inspeções.

- **[BAIXA] `type: data.type as never`** — `linha 54` · _Categoria: Tipagem_
  Por quê: Cast `as never` substitui a correção do tipo do parâmetro.
  Direção: Adicionar o tipo correto de enum/union ao parâmetro para eliminar o cast.

### `apps/api/src/modules/reports/repo.ts` — peso 2

- **[MÉDIA] Resultados SQL raw castados como `unknown as { rows: T[] }`** — `linha 12` · _Categoria: Tipagem_
  Por quê: Quatro resultados de SQL raw são castados sem validação em runtime; drift SQL/tipo é invisível.
  Direção: Validar com Zod ou usar tipos explícitos do driver para os resultados raw.

### `apps/api/src/core/ai/registry.ts` — peso 3

- **[MÉDIA] `signature` mapeado para `geoHandler` sem contexto** — `linha 28` · _Categoria: Legibilidade_
  Por quê: A delegação de `signature` para `geo` é não óbvia; sem comentário o leitor não entende a relação.
  Direção: Adicionar comentário explicando a delegação ou renomear para tornar a associação explícita.

- **[BAIXA] `HandlerResult.validation` tipado como `Record<string, unknown>`** — `linha 28` · _Categoria: Tipagem_
  Por quê: Callers precisam re-cast por kind sem contrato garantido entre os tipos de handler.
  Direção: Usar discriminated union por tipo de handler em `HandlerResult`.

### `apps/api/src/modules/evidences/service.ts` — peso 2

- **[MÉDIA] Status literal `'conforme'` ao aceitar evidência** — `linha 102` · _Categoria: Magic Values_
  Por quê: Mesma string de domínio repetida sem fonte única de verdade.
  Direção: Substituir por `ITEM_STATUS.conforme`.

### `apps/api/src/modules/auth/route.ts` — peso 2

- **[MÉDIA] Limites de rate-limit de login/refresh como literais** — `linha 13` · _Categoria: Magic Values_
  Por quê: `max: 10` e `max: 20` nos endpoints de auth sem nomes que expliquem a política de segurança; os valores diferem sem justificativa documentada.
  Direção: Definir `LOGIN_RATE_LIMIT_MAX = 10` e `REFRESH_RATE_LIMIT_MAX = 20`.

### `apps/api/src/app.ts` — peso 2

- **[MÉDIA] Rate-limit global `max: 10` e `'1 minute'` inline** — `linha 35` · _Categoria: Magic Values_
  Por quê: Defaults de rate-limit global sem nome; uma mudança de política exige buscar o literal.
  Direção: Extrair para `GLOBAL_RATE_LIMIT_MAX` e `GLOBAL_RATE_LIMIT_WINDOW`.

### `apps/api/src/test/inspections.test.ts` — peso 2

- **[MÉDIA] `beforeAll` de setup duplicado em `evidences.test.ts`** — `linha 17` · _Categoria: Duplicação_
  Por quê: Bloco que cria veículo, template e inspeção via HTTP inject é quase idêntico em `inspections.test.ts:17-41` e `evidences.test.ts:36-70`.
  Direção: Adicionar `seedInspection(app, gestor, vistoriador)` em `test/helpers.ts`.

### `apps/api/src/modules/vehicles/repo.ts` — peso 1

- **[BAIXA] `status` tipado como `string` em `updateVehicle`** — `linha 44` · _Categoria: Tipagem_
  Por quê: Aceita qualquer string; um valor inválido de status passaria sem erro.
  Direção: Restringir ao union literal de status válidos.

### `apps/api/src/modules/reports/service.ts` — peso 1

- **[BAIXA] Quatro funções exportadas sem tipo de retorno** — `linha 9` · _Categoria: Tipagem_
  Por quê: API pública do módulo sem contrato explícito; callers dependem de inferência.
  Direção: Anotar tipos de retorno nas funções exportadas.

### `apps/api/src/modules/inspections/route.ts` — peso 1

- **[BAIXA] Renomeação de campos de query e parsing de `Date` inline no handler** — _Categoria: Legibilidade_
  Por quê: Conversão de query params para filtros feita inline no handler dificulta testar a lógica de transformação isoladamente.
  Direção: Extrair `queryToFilter(query)` mapper testável.

### `apps/api/src/core/errors/error-handler.ts` — peso 1

- **[BAIXA] Códigos HTTP `401` e `429` como literais no catch** — `linha 32` · _Categoria: Magic Values_
  Por quê: Inconsistente com `AppError` que carrega status nomeado; checks numéricos são frágeis.
  Direção: Definir constantes `HTTP_STATUS` ou derivar valores do factory `AppError`.

## Não aplicável

- **N+1**: Nenhum padrão detectado — Drizzle é usado com queries pontuais nos repos; não há loops com queries DB dentro.
- **Tipagem**: Aplicável a todos os 45 arquivos de produção TypeScript revisados.

---
_Relatório somente informativo — nenhum arquivo foi modificado._
