# Testes, cobertura e logs — 2026-06-19 01:03

**Escopo:** session (`packages/*`)  ·  **Framework:** Vitest 2.1.9
**Comando:** `pnpm test`

## Suíte

| Pacote        | Arquivos | Testes | Resultado     |
| ------------- | -------- | ------ | ------------- |
| `contracts`   | 8        | 87     | ✓ todos passaram |
| `ui`          | 40       | 158    | ✓ todos passaram |
| **Total**     | **48**   | **245** | **✓ verde**  |

Antes desta sessão: `contracts` tinha 1 arquivo / 5 testes; `ui` tinha 40 arquivos / 149 testes (total: 154).
Ganho: **+91 testes**.

## Cobertura

`@vitest/coverage-v8` não está instalado — métricas de linha/branch indisponíveis.

> Para habilitar: `pnpm add -D @vitest/coverage-v8` (raiz ou por pacote) e rodar `pnpm test --coverage`.

## Testes adicionados

### contracts

- `src/auth.test.ts` *(novo, 10 testes)* — `passwordSchema` (valid/short), `loginInput` (valid/invalid email/short pwd), `tokenPair`, `jwtPayload` (valid/unknown role/null tenantId), `meOutput`.
- `src/pagination.test.ts` *(novo, 9 testes)* — constantes `DEFAULT_PAGE_SIZE=20` / `MAX_PAGE_SIZE=100`, coerção string→number, defaults, rejeição de `limit>100` e `limit<1`, `cursor` uuid.
- `src/user.test.ts` *(novo, 12 testes)* — `createUserInput` com `ASSIGNABLE_ROLES`, rejeição de `superadmin`, senha curta, email inválido; `updateUserInput` aceita campos parciais, ignora senha em update.
- `src/vehicle.test.ts` *(novo, 13 testes)* — todos os valores de status enum, default `km=0` / `status=disponivel`, km negativo rejeitado, atualização parcial.
- `src/checklist.test.ts` *(novo, 12 testes)* — `createChecklistTemplateInput`: aninhamento de itens e requisitos, rejeição de label/nome vazio.
- `src/inspection.test.ts` *(novo, 15 testes)* — tipos de inspeção, campos de evidência, `auditInput` aceita somente `"aprovada"` / `"reprovada"`, rejeição de decisão inválida.
- `src/tenant.test.ts` *(novo, 11 testes)* — `createTenantInput`: regex de slug (sem maiúsculas/espaços/especiais), senha do gestor mínimo 6 chars, boundary exato.

### ui

- `src/domain/UniqueCode/UniqueCode.web.test.tsx` *(editado)* — corrigido vazamento de stub de `navigator` (adicionado `afterEach(() => vi.unstubAllGlobals())`); substituído teste de timer real por `vi.useFakeTimers()` + `vi.advanceTimersByTime(1500)` para validar que "Copiado!" some após o timeout.
- `src/atoms/Input/Input.web.test.tsx` *(editado, +1 teste)* — branch `type="datepicker"`: renderiza `<input type="date">` com valor correto.
- `src/tokens/index.test.ts` *(editado, +4 asserções)* — novos tokens de cor (`successBg`, `primaryBg`, `warningBg`, `errorBg`, `neutralBg`), novos valores de espaçamento (`xxs`, `2xl`, `3xl`), novo objeto `borderRadius` (pill/button/card/image), novo objeto `size` (vehicleImageHeight/progressTrackHeight).
- `src/molecules/UploadArea/UploadArea.web.test.tsx` *(editado, +4 testes)* — arquivo inválido (PDF) não chama `onFiles`; drop misto (PNG+PDF) entrega só o válido; atributo `accept` do input contém somente `image/png,image/jpeg`; label "10MB" visível.

## Lacunas de cobertura priorizadas (não escritas)

Nenhuma lacuna de alta prioridade restante no escopo desta sessão.

Cobertura de branches em `packages/db` e `packages/api-client` não foi avaliada por ausência da ferramenta de coverage.

## Achados — qualidade de testes

- **[MÉDIO] Vazamento de stub de navigator** — `packages/ui/src/domain/UniqueCode/UniqueCode.web.test.tsx`
  Por quê: `vi.stubGlobal` sem `afterEach` polui o ambiente de testes subsequentes.
  Direção: **corrigido nesta sessão** (adicionado `afterEach(() => vi.unstubAllGlobals())`).

- **[MÉDIO] Timer real em teste de UI** — `packages/ui/src/domain/UniqueCode/UniqueCode.web.test.tsx:25`
  Por quê: `findByText` aguardava 1500ms reais, tornando o teste lento e frágil.
  Direção: **corrigido nesta sessão** (fake timers com `vi.useFakeTimers()`).

## Achados — logging

- **[ALTA] Senha em texto plano no stdout** — `packages/db/src/seed.ts:44`
  Por quê: `console.log` imprime a senha literal `"senha123"` durante o seed. Logs são frequentemente coletados em sistemas de observabilidade e podem vazar credenciais.
  Direção: remover o log ou substituir por `"gestor password: [REDACTED]"`. A senha deveria vir de variável de ambiente, não estar hardcoded.
  > ⚠ **Esta skill não altera código de produção.** Correção pendente — requer intervenção manual.

## Não aplicável / nada encontrado

- Logging excessivo em `packages/contracts` e `packages/ui` — nenhum achado (sem acesso a I/O nesses pacotes).
- N+1 queries — não aplicável (pacotes sem ORM/DB no escopo desta execução).

---
_Esta skill só cria/edita arquivos de teste. Nenhum código de produção foi alterado._
