# Revisão de Higiene de Código — apps/web
**Data:** 2026-06-18 20:17  
**Escopo:** `apps/web/` — 52 arquivos fonte (branch `feat/api`)  
**Categorias:** Valores mágicos · Código morto · Duplicação · N+1 · Tipagem · Legibilidade

---

## Resumo

| Severidade | Total |
|------------|-------|
| 🔴 Alta     | 2     |
| 🟡 Média    | 9     |
| 🟢 Baixa    | 10    |
| **Total**  | **21** |

**Nenhum código morto** ou **N+1** encontrado.

---

## Achados por arquivo (pior primeiro)

---

### `apps/web/src/app/(dashboard)/checklists/page.tsx` · `reports/page.tsx` · `users/page.tsx`
_Peso: 🔴 Alta — padrão repetido em 3 arquivos_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 1 | 7–10 | 🔴 Alta | Padrão auth+RBAC repetido em todas as server pages | `getServerSession → redirect → can() → mensagem de negação` copiado em checklists, reports e users. Qualquer mudança na política exige atualização em 3 lugares. | Extrair um wrapper de servidor (ex.: `withRoleGuard(action, page)`) que encapsula a sequência e retorna a UI ou a mensagem padrão de acesso negado |

---

### `apps/web/src/app/api/session/route.ts`
_Peso: 3+2+1 = 6_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 2 | 11 | 🔴 Alta | Número mágico no max-age do cookie | `60 * 60 * 24 * 7` é opaco; sem o comentário adjacente o valor `604800` não diz nada — e o comentário diz *o que* (7 dias), não *por que* coincide com o lifetime do refresh token. _(magic-values + readability)_ | Extrair `REFRESH_TOKEN_LIFETIME_SECONDS` no nível do módulo com comentário apontando para spec/backend |
| 3 | 52–70 | 🟡 Média | Configuração do cookie duplicada em POST e DELETE | Campos `httpOnly`, `secure`, `sameSite`, `path` repetidos em ambos os handlers — mudança deve ser feita em dois lugares. | Extrair objeto `SESSION_COOKIE_OPTIONS` e reusá-lo nos dois handlers |
| 4 | 14 | 🟢 Baixa | Catch sem tipagem em `request.json()` | `catch` implicitamente `any`; falha de parse não tem tipo conhecido. | Usar `catch (err: unknown)` e estreitar o tipo antes de usar |

---

### `apps/web/src/lib/session.ts`
_Peso: 2+2 = 4_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 5 | 32 | 🟡 Média | Cast sem validação prévia do shape | `JSON.parse(json)` castado para `Partial<WebSession>` sem narrowing — formato desconhecido até checar. _(typing + readability)_ | Validar o shape do objeto parseado antes do cast, ou usar Zod para garantir a forma |

---

### `apps/web/src/components/app-sidebar.tsx`
_Peso: 2+2 = 4_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 6 | 48–49 | 🟡 Média | `as any` em `router.push` com eslint-disable sem explicação | O cast contorna o TypeScript sem contexto de por que é necessário. _(typing + readability)_ | Resolver o mismatch de tipo corretamente, ou adicionar comentário explicando a incompatibilidade da API do Next.js |

---

### `apps/web/src/lib/web-api.ts`
_Peso: 2+1 = 3_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 7 | 72 | 🟡 Média | `JSON.parse` retorna `any`, castado direto para `T` | Segurança de tipo perdida em boundary público da API — shape desconhecido até runtime. | Aceitar `unknown`, validar o shape antes de retornar como `T` |
| 8 | 125 | 🟢 Baixa | Variável `c` como nome de função de chamada | Nome de uma letra impossibilita grep e contexto imediato. | Renomear para `call` ou `makeRequest` |

---

### `apps/web/src/components/report-charts.tsx`
_Peso: 2+1 = 3_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 9 | 20 | 🟡 Média | `ChartCard` instanciado 3× com estrutura idêntica | Título e dados variam; o resto é igual. Adicionar um 4º gráfico exige cópia manual. | Mover para array de `{ title, data, dataKey }` e mapear |
| 10 | 33 | 🟢 Baixa | `height: 260` hardcoded no estilo inline | Não está alinhado ao design system; parece arbitrário. | Extrair para constante `CHART_HEIGHT_PX` ou variável CSS |

---

### `apps/web/src/components/users-table.tsx`
_Peso: 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 11 | 22 | 🟡 Média | Padrão de `onSuccess` de mutation idêntico em `users-table` e `vehicles-table` | `onSuccess: () => qc.invalidateQueries({ queryKey: [...] })` copiado em ambos. | Extrair factory de mutation que recebe a query key e gera o handler padrão |

---

### `apps/web/src/components/user-form-dialog.tsx`
_Peso: 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 12 | 36 | 🟡 Média | Markup de campo de formulário repetido entre dialogs | `<div className="space-y-2"><Label/><HtmlInput/></div>` repetido 7+ vezes entre `user-form-dialog` e `vehicle-form-dialog`. | Criar componente `FormField` com props `label`, `type`, `value` |

---

### `apps/web/src/lib/query-client.ts`
_Peso: 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 13 | 6 | 🟡 Média | Número mágico `30000` para stale time | Decisão de negócio (30 s de cache) sem nome. | Extrair para `QUERY_STALE_TIME_MS` |

---

### `apps/web/src/components/login-form.tsx`
_Peso: 1+1 = 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 14 | 27 | 🟢 Baixa | Catch sem tipo explícito | `catch` implícito `any` — erros não têm tipo conhecido. | Usar `catch (err: unknown)` |
| 15 | 31 | 🟢 Baixa | Catch sem tipo em chain de promises | Mesmo problema em `.catch(() => null)` silencioso. | Declarar tipo explicitamente ou converter para try/catch |

---

### `apps/web/src/components/inspection-laudo.tsx`
_Peso: 1+1 = 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 16 | 7 | 🟢 Baixa | `EvidenceView` sem docstring | Função auxiliar interna sem contexto do que renderiza. | Adicionar JSDoc de uma linha descrevendo o propósito |
| 17 | 29 | 🟢 Baixa | `ItemView` sem docstring | Mesmo problema. | Idem |

---

### `apps/web/src/components/template-editor.tsx`
_Peso: 1+1 = 2_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 18 | 38 | 🟢 Baixa | Variável `reqLabels` com formato implícito | Nome não indica que o valor é uma string separada por vírgulas. | Renomear para `requiredLabelsStr` ou `expectedLabelsCSV` |
| 19 | 25 | 🟢 Baixa | Componente sem docstring | Gerenciamento de estado complexo sem contexto. | Adicionar JSDoc descrevendo fluxo de criação de template |

---

### `apps/web/src/components/vehicles-table.tsx`
_Peso: 1_

| # | Linha | Sev | Título | Por que prejudica | Direção |
|---|-------|-----|--------|-------------------|---------|
| 20 | 14 | 🟢 Baixa | Componente sem docstring | Props e comportamento (CRUD role-gated) não documentados. | Adicionar JSDoc descrevendo responsabilidades |

---

## Não aplicável

- **N+1 queries** — `apps/web` não tem acesso direto a ORM/DB; chamadas via `serverApi()` / `browserApi()`. Não aplicável.
- **Código morto** — nenhum encontrado.
